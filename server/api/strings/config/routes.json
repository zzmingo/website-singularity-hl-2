{
  "routes": [
    {
      "method": "GET",
      "path": "/strings",
      "handler": "Strings.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/strings/count",
      "handler": "Strings.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/strings/:id",
      "handler": "Strings.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/strings",
      "handler": "Strings.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/strings/:id",
      "handler": "Strings.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/strings/:id",
      "handler": "Strings.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
